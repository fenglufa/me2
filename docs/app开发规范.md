
---

# 📂 Flutter 前端项目结构与规范

```
fmd_app/
├── assets/                # 静态资源
│   ├── images/            # 图片资源
│   ├── fonts/             # 字体资源
│   └── lottie/            # 动画文件
│
├── lib/
│   ├── main.dart          # 应用入口
│   ├── app.dart           # App 根部配置（Theme、Router、Provider）
│   │
│   ├── config/            # 全局配置
│   │   ├── app_config.dart
│   │   ├── env.dart       # 环境变量
│   │   └── theme.dart     # 主题配置（亮色 / 暗色）
│   │
│   ├── core/              # 核心模块（框架级）
│   │   ├── network/       # 网络层（Dio 封装、拦截器）
│   │   ├── storage/       # 本地存储（SharedPreferences / SecureStorage）
│   │   ├── utils/         # 工具类（时间、格式化、加密）
│   │   ├── constants/     # 常量（API 地址、Key 值）
│   │   └── logger.dart    # 日志工具
│   │
│   ├── common/            # 公共组件 & 通用逻辑
│   │   ├── widgets/       # 全局复用组件（按钮、输入框、加载框）
│   │   ├── styles/        # 公共样式
│   │   ├── mixins/        # 混入
│   │   └── extensions/    # 扩展方法
│   │
│   ├── modules/           # 功能模块（业务相关）
│   │   ├── auth/          # 登录 / 注册 / 权限
│   │   │   ├── model/
│   │   │   ├── service/
│   │   │   ├── controller/
│   │   │   └── view/
│   │   │
│   │   ├── diary/         # 日记模块（日记本 + 日记）
│   │   │   ├── model/     # 数据模型
│   │   │   ├── service/   # 业务逻辑、API
│   │   │   ├── controller/# 状态管理（Riverpod/Bloc/GetX）
│   │   │   └── view/      # UI 页面（含 widgets 子目录）
│   │   │
│   │   ├── channel/       # 频道（话题流、卡片）
│   │   ├── profile/       # 个人中心
│   │   ├── explore/       # 探索（未来扩展）
│   │   └── settings/      # 设置
│   │
│   ├── router/            # 路由管理（统一入口）
│   │   └── app_router.dart
│   │
│   └── l10n/              # 多语言支持
│       └── arb/           # Flutter Intl 格式
│
├── test/                  # 单元测试 & 集成测试
│
├── pubspec.yaml           # Flutter 依赖配置
└── README.md              # 项目说明
```

---

# 📌 开发规范

### 1. **模块化**

* 所有业务功能放在 `modules/`，每个模块自带：

  * `model/` → 数据模型
  * `service/` → 网络请求 + 核心逻辑
  * `controller/` → 状态管理
  * `view/` → 页面 & 组件
* 避免「万能大文件」，模块间尽量独立

---

### 2. **文件规范**

* 单文件 ≤ 500 行
* 单方法 ≤ 50 行
* 超过必须 拆分子类/子文件
* 文件命名 → 下划线命名法（`card_service.dart`）

---

### 3. **命名规范**

* 类名 → UpperCamelCase（`CardService`）
* 方法 → lowerCamelCase（`fetchCards()`）
* 常量 → 全大写下划线（`API_BASE_URL`）
* 文件夹/文件 → 小写下划线（`card_detail_page.dart`）

---

### 4. **状态管理**

* 推荐使用 **Riverpod** 或 **Bloc**，避免在 View 层直接写业务逻辑
* Controller 只负责 **状态 & 调用 service**

---

### 5. **网络请求**

* 所有请求必须经过 `core/network/`
* 统一在 `DioClient` 内加拦截器（日志、鉴权、错误处理）
* 不要在 View 里直接写 `Dio.get(...)`

---

### 6. **日志**

* 统一使用 `core/logger.dart`
* 按模块打印，方便定位问题
* 生产环境可配置为上传到后端

---

### 7. **配置管理**

* 所有 API 地址、Key、主题颜色 → 放在 `config/`
* 不要在代码中硬编码 URL / Key

---

### 8. **可扩展性**

* 每个模块必须写 `README.md`，说明该模块职责、依赖和调用方式
* 提供统一入口（例如 `CardService` 代理 `CardPermissionService` / `CardReportService`）

---

这样，前端项目会**清晰、易扩展、避免代码膨胀**，同时便于多人协作。

---
modules/card/` 目录示例：

modules/
└── card/
    ├── controller/          # 状态管理（Flutter 端）
    │   ├── card_list_controller.dart
    │   ├── card_detail_controller.dart
    │   └── card_action_controller.dart   # 处理编辑/删除/举报等操作
    │
    ├── service/             # 服务层（API 请求 & 业务逻辑）
    │   ├── card_service.dart
    │   ├── card_permission_service.dart
    │   └── card_report_service.dart
    │
    ├── view/                # 页面
    │   ├── card_list_page.dart
    │   ├── card_detail_page.dart
    │   └── widgets/
    │       ├── card_item.dart
    │       └── card_action_menu.dart
    │
    ├── model/               # 数据模型
    │   ├── card.dart
    │   ├── card_permission.dart
    │   └── card_report.dart
    │
    └── README.md            # 模块说明
